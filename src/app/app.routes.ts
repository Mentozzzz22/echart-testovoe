import { Routes } from '@angular/router';
import {ChartPaintingFormComponent} from './chart-painting-form/chart-painting-form.component';

export const routes: Routes = [
  { path: '', component: ChartPaintingFormComponent },
];
